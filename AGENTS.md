# CHFS 项目协作约定

## “同步”快捷指令

当用户以命令形式说“同步”时，表示执行以下安全同步流程：

1. 检查当前分支、工作区修改、未跟踪文件和远程地址；
2. 使用项目已配置的 GitHub SSH 远程执行 `fetch --prune`；
3. 工作区干净且本地仅落后时，使用 `merge --ff-only` 快进到远程 `main`；
4. 本地存在修改、分支发生分叉或未跟踪文件可能被覆盖时，不擅自清理、暂存或合并，先说明具体冲突；
5. 完成后报告本地提交、远程提交、最新标签以及工作区状态。

解释或讨论“同步”这个词不构成执行授权；只有把“同步”作为动作指令使用时才执行。

## “发布 / 发版”快捷指令

当用户以命令形式说“发布”或“发版”时，表示在当前明确要求的代码修改完成后，继续执行完整发布流程，无需再次询问是否需要以下常规步骤：

1. 拉取并核对远端 `main`，保留所有不属于本次任务的用户文件和未跟踪文件；
2. 运行相关测试、完整单元测试与 `compileall`；
3. 递增补丁版本号，更新发布说明和发布检查记录；
4. 只暂存本次修改，提交并推送现有 `main`，除非用户另行要求分支或 PR；
5. 使用 `scripts\build_exe.cmd` 构建桌面 `CHFS.exe` 与 `SHA256SUMS.txt`；
6. 实际启动 EXE，确认 `/api/health` 返回本次版本，并核对 SHA-256；
7. 创建并推送对应 `v*` 标签，通过 `gh release create` 上传桌面 EXE 与校验文件；
8. 反查 GitHub Release，确认它是 Latest、附件摘要正确、标签与远端 `main` 指向发布提交。

仅在用户把“发布”或“发版”作为动作指令使用时执行上述流程；解释或讨论这些词不构成发布授权。涉及覆盖 Release、改写既有远端标签、删除文件、关闭安全软件或其他超出常规发布流程的高风险操作时，仍需单独说明并按权限规则处理。
